// BFS 模板

加入一個元素到queue裡面
while (q不為空) {
	獲得移除前的qsize
	for (0 to qsize) {
		cur = q.remove() 移除q中的頭元素
		做一些你需要的操作 例如印出樹的走訪順序之類(真正在操作在這)

		for (走訪cur(頭元素)的兒子) {
			加入到queue中 這裡是探索 然後加入queue, 還沒有對她做操作
		}
	}


}

// DFS 模板

main() {
	
	helper();
}

helper() {
	// 結束條件

	// 遞回式

	// 回傳值
}